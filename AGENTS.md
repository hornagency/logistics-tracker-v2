# Agent / continuity notes (Vectora v2)

- **Product:** Vectora Logistics — domain `vectoralogistics.com`, tracking prefix **`VLA-`**, admin session cookie **`vla_session`**.
- **Repo:** `origin` → `https://github.com/hornagency/logistics-tracker-v2.git`; optional **`upstream`** → `hornagency/logistics-tracker` (legacy template — do not merge branding or copy from it into this product).
- **Theme:** Teal primary (`#0d9488` / `#0f766e` in Tailwind + `globals.css`).
- **Env (prod):** `DATABASE_URL`, `ADMIN_USERNAME`, `ADMIN_PASSWORD`, `ADMIN_SECRET`, `NEXT_PUBLIC_SITE_URL`.
- **Sibling folder:** `../logistics-tracker` is a separate legacy project in the same workspace — keep Vectora-only assets and copy in this repo only.

Open **`/Users/0x/Logistics.code-workspace`** in Cursor to work in one window with both projects.
